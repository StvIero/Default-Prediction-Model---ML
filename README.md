# Default-Prediction-Model---ML
The goal of this assignment is to build a credit scoring model (or, rather, default prediction model) on the basis of logistic regression and machine learning methods.
